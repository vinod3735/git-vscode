name: First Workflow
on: [push]

jobs:
    run-shell-commands:
        runs-on: ubuntu-latest
        steps:
            - name: echo hello world
              run: echo "Hello World"
            - name: multiline cmd
              run: |
                   node -v
                   npm -v
